# Normalize line endings: store LF in the repo, check out native.
# Kills the CRLF/LF "hundreds of modified files" noise on the Windows clone.
* text=auto eol=lf

# Binary assets — never normalize
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.woff binary
*.woff2 binary
